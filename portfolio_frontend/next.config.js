/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Using local images only; add remotePatterns here if needed later.
  }
};

module.exports = nextConfig;
